export default function SoonBadge({ size }) {
  return (
    <div
      style={{ fontSize: size }}
      className="flex items-center justify-center  text-gray-lighter rounded font-bold"
    >
      <span>SOON</span>
    </div>
  );
}
